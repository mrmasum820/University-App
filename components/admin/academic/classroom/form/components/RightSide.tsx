"use client";

import React from "react";
import Publish from "./Publish";

const RightSide = () => {
  return (
    <div>
      {/* publish */}
      <Publish />
    </div>
  );
};

export default RightSide;
